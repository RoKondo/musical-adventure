version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          cd dockerK
          docker build -t rodrigokondo/cartahub:latest .
          docker save -o image.tar  rodrigokondo/cartahub:latest
      - persist_to_workspace:
          root: /home/circleci/project/docker
          paths:
            - image.tar
  push-image:
    docker:
      - image: cimg/base:stable
    steps:
      - attach_workspace:
          at: /tmp
      - setup_remote_docker
      - run: |
          docker load -i /tmp/image.tar
          docker images
          echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin
          docker push rodrigokondo/cartahub:latest
workflows:
  version: 2
  build_push_docker:
    jobs:
      - build
      - push-image:
          requires:
            - build